```mermaid
flowchart TD
    A(房价泡沫) --> B{要素成本高涨}
    B --> C(挤出民营企业)
    C --> D{民营企业衰退}
    D --> E(失业增加, 收入下降)
    E --> F{社会需求萎缩}
    F --> G{房地产过剩}
    G --> H{投资减少}
    H --> E
    F --> I{通缩螺旋}
    I --> J{物价下降, 工资下降}
    J --> B
    I --> K{政府干预失效}
    K --> C

    subgraph 经济结构自锁
        A == 结构自锁 ==> B ==> C ==> D ==> E ==> F ==> G ==> H == 结构自锁 ==> E
    end

    subgraph 通缩螺旋
        I -. 通缩 .-> J -.-> B -.-> C -.-> D -.-> E -.-> F -. 通缩 .-> I
    end
